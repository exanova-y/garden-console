# Maintained Content

The curated Feedly/RSS source list will live here as `feeds.opml`. Keep source
selection as data; introduce custom source adapters only when a chosen source
does not expose usable RSS or Atom.
